    // search = strstr(text1, PARAM) + PARAM_length + 1;
    
    // strcpy(text2, search);
    // text1[strlen(text1)-strlen(text2)]='\0'; //cuts the whole string removing the length of text 2

    // int num = 0;
    // while(text2[num] != '\"'){
    //     text2[num] = '0';
    //     num++;
    // }

    // strcat(text1,text2);//concat the strings back